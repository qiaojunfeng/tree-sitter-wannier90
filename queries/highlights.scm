[
  (identifier)
  "identifier"
] @variable

[
  (coordinate_type)
  (sph_ham)
  "keyword"
] @keyword

(boolean) @constant.builtin.boolean

(comment) @comment

(number) @constant.numeric

(range) @string

[
    "."
    ","
    ";"
] @punctuation.delimiter

[
  "="
  ":"
] @operator

;[
;    "["
;    "]"
;    "{"
;    "}"
;] @punctuation.bracket
